import { dataProducts } from "./dataProducts";

export function putProducts(){
    const products = dataProducts.products;
    const itemsProducts = document.querySelector('.products__items-products');

    
}